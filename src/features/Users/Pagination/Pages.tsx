import React from "react"
import { styled } from "@mui/material"

const Container = styled("div")({
  display: "flex",
  
})

export const Pages = () => {
  return (
    <Container>
    </Container>
  )
}
